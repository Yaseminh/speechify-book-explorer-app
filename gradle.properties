# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# JVM arguments (daha fazla bellek - Compose build?lerinde laz?m)
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8 -Dkotlin.daemon.jvm.options=-Xmx2g

# Parallel build h?zland?rmas? (iste?e ba?l?)
org.gradle.parallel=true

# AndroidX kullan?m? zorunlu
android.useAndroidX=true

# Kotlin kod stili
kotlin.code.style=official

# Daha küçük R s?n?f? üretimi
android.nonTransitiveRClass=true

# ? Kotlin 2.x ve Compose için önemli:
# K2 derleyicisini kapatma sat?rlar? **olmamal?**.
# E?er daha önce a?a??dakilerden biri varsa, sil:
# kotlin.experimental.tryK2=false

# ? Compose compiler hata mesajlar?n? netle?tirmek için (opsiyonel ama faydal?)
android.defaults.buildfeatures.compose=true

